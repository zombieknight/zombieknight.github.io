function snowingAnim(){
	for (int i = 0; i < arraySize; i++) {
    
    x = random(26, width-26);
    y = random(26, height-26);
    ellipse(x, y, circleSize, circleSize);
  }

}

function shootingStar(){

}